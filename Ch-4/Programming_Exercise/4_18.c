/*
Write the program to compute the value of the expression x=a-b/3+c*2-1
*/

#include<stdio.h>

void main()
{
    int x,a,b,c;

    // asign value to a,b,c
    a=2;
    b=5;
    c=6;

    x= (a) - (b)/ (3) + (c)  * (2) - (1) ;

    printf("The value of X = %d",x); 
}